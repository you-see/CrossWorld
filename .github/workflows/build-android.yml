name: Build Android APK

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "================================"
          echo "CrossWorld Android Workflow OK"
          echo "================================"
          echo "Ubuntu:"
          uname -a